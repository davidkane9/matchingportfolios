
randPort <- function(data, match.var, weight.var) {
    x0 = data[[weight.var]]
    Emat = matrix(1, ncol = nrow(data), nrow = 1)
    mW = getWeights(Emat, x0, 1)
    rP = new("randPort", match.var = match.var, weight.var = weight.var, matched.weights = mW, universe = data)
    return(rP)
}
